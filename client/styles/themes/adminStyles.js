import { createTheme } from "@mui/material/styles";

const ADMIN_BASIC_THEME = createTheme({
  palette: {
    primary: {
      main: "#122c34",
      darker: "#398EA7",
    },
  },
});

export default ADMIN_BASIC_THEME;
